The folder named "database" in this directory (except README.md) is reserved for PostgreSQL mounted database.

If you want to wipe the entire database, do remove the "database" folder.
This can fix issues related to PostgreSQL.